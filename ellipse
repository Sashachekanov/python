x = 0
y = 0

def setup():
    size(600,600)
    frameRate (100)
    
    
def draw():
    clear()
    global x, y
    clear()
    ellipse(x,x,100,100)
    if x == 0:
        y = 1
    if y == 1:
        x = x + 1
    if x == 600:
        y = 0
    if y == 0:
        x = x - 1
        

        
